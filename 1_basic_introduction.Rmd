## введение 
Что, зачем и почему.
## Установка R

## GUIs

## простые вычисления

```{r}
log(10)

```
## простые функции log, sum, exp...

## `?` справка по функциям

## назначение объектов
```{r}
A <- 3.14
```
## операции с объектами
```{r}
A

A+10
A^2
sqrt(A)

A^0.5

A^(1/2)

rm(A)

A
```
## вектор, конкатенация
```{r}
B<-c(1,21,31,5) # создаем вектор
```
## subsetting vector
```{r}
B*2

B[1] # первый элемент из B

B[B>10]

B[B!=10]

C<-B[B!=10]

B[1]<-6

B<-c(B,6)

B<-c(B[1:3], 6, B[4:length(B)])

B<-B[-1]

Wing<-rnorm(200, 30, 5)
Wing<-rnorm(n=200, mean=30, sd=5)
Wing<-rnorm(n=200, sd=5, mean=30)
```
## гистограмма, статистика вектора
```{r}
hist(Wing, breaks=20)

mean(Wing)
sd(Wing)

summary(Wing)
```
## data.frame
```{r}
Swifts<-data.frame(ID=1:200, 
    Wing=Wing, 
	Sex=c(rep("Male", 100), rep("Female", 100)))
```
## subset..

# head, tail, summary. plot

## ls()

## save() load()

## getwd() setwd()

## пакеты - библиотеки
